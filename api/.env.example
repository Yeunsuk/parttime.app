# 서버에서 .env로 복사해서 실제 값 채우기. .env 자체는 커밋 금지 (.gitignore 확인).

# 앱 런타임 (필수, 비밀값)
DB_PASSWORD=
JWT_SECRET=
OWNER_AUTH_CODE=

# 앱 런타임 (내부 설정 — DB 컨테이너의 --network 내부 호스트명)
DB_HOST=

# 배포 스크립트 전용 (deploy-from-registry.sh가 읽음)
IMAGE=
CONTAINER_NAME=
NETWORK_NAME=
HOST_PORT=
